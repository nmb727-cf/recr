from django.apps import AppConfig

class AutomationChangeImpactConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.automation_change_impact'
    verbose_name = 'Automation Change Impact Center'
