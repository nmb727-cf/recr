from django.apps import AppConfig


class AutomationPlaybooksConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.automation_playbooks'
    verbose_name = 'Automation Playbooks'
