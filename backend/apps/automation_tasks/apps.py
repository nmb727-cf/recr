from django.apps import AppConfig


class AutomationTasksConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.automation_tasks'
    verbose_name = 'Automation Task Orchestration'
