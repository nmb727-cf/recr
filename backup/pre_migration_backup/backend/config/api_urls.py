from django.urls import path, include

urlpatterns = [
    # Auth endpoints
    # path('auth/', include('apps.accounts.urls')),
    
    # Core endpoints  
    # path('jobs/', include('apps.jobs.urls')),
    # path('candidates/', include('apps.candidates.urls')),
    
    # More endpoints added as apps are built
]